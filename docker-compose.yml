version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000' # Adjust the host port if your app runs on a different internal port
    environment:
      - NODE_ENV=production
    restart: unless-stopped
